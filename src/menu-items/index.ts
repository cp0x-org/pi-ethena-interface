import { NavItemType } from 'types';

// ==============================|| MENU ITEMS ||============================== //

// The current interface uses custom header links instead of navigation-driven menus,
// so we export an empty menu configuration to avoid unused menu definitions.
const menuItems: { items: NavItemType[] } = {
  items: []
};

export default menuItems;
